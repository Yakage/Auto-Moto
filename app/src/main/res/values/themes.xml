<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AutoMoto" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@color/blue</item>
    </style>

    <style name="Theme.AutoMoto" parent="Base.Theme.AutoMoto" />
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_auto</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.AutoMoto</item>
    </style>
</resources>
